{
  "name": "tv_app",
  "version": "1.0.0",
  "description": "Using TV Maze API for web app",
  "main": "index.js",
  "scripts": {
    "install-all": "yarn install && cd client && yarn install",
    "client": "cd client && yarn start",
    "server": "nodemon server.js",
    "local": "concurrently --kill-others-on-fail \"yarn server\" \"yarn client\""
  },
  "dependencies": {
    "async": "^3.2.0",
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "nodemon": "^2.0.6",
    "request": "^2.88.2",
    "request-promise": "^4.2.6",
    "send": "^0.17.1"
  },
  "devDependencies": {
    "concurrently": "^3.5.0"
  }
}
